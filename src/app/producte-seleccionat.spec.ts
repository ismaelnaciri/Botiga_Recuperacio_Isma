import { ProducteSeleccionat } from './producte-seleccionat';

describe('ProducteSeleccionat', () => {
  it('should create an instance', () => {
    expect(new ProducteSeleccionat()).toBeTruthy();
  });
});
